# 会话记录 — 2026-07-02

## 完成的工作

### 1. 模型去重 & 清理 (188 → 146)
- 修复 42 个模型的缺失 hyperparameters（从名称提取 algorithm）
- 回填 49 个模型的缺失描述
- 删除 24 个重复模型（同 dataset+algo 保留最佳）
- 删除 1 个孤立草稿模型
- 删除 14 个 perfect_accuracy 过拟合模型
- 删除 4 个 missing_f1 多数类预测器

### 2. Bug 修复

#### 2a. `diagnose_model_quality.py` NULL f1 漏检
- **文件**: `app/services/parameter_guidance_service.py` (未改)
- **文件**: `scripts/diagnose_model_quality.py:212-228`
- 新增 `missing_f1` 检测：高准确率 + f1=NULL → Tier 1
- 新增 `majority_class_predictor` 检测：acc>=0.9 + f1<0.01 → Tier 1
- `missing_f1`, `majority_class_predictor` 加入 `definite_reasons` 集合
- `metrics_dict` 变量提前到 f1 检查之前

#### 2b. 训练代码 bug
- **`app/executor/trainers/sklearn_trainer.py:152`** — `load_data()` 开头加 `df.drop_duplicates()` 防数据泄露
- **`app/executor/trainers/sklearn_trainer.py:1014-1026`** — 6 个指标拆独立 try-except
- **`app/executor/trainers/transformers_nlp_trainer.py:368-379`** — 同上

#### 2c. 数据集分析页 500 错误
- **`app/routes/api/datasets.py:335-358`** — 新增 `_convert_numpy_types()` 递归转 numpy→Python 原生
- 根因: `DatasetAnalyzer` 产生 `np.False_`（numpy bool），Flask jsonify 无法序列化
- 表现: 数据集详情页 → AI 智能推荐 → 点分析 → 一直加载失败

#### 2d. 训练创建 → 调优页面参数丢失
- **`app/routes/training.py:103-138`** — session presets 从 8 字段扩展到 18 字段
- **`app/templates/training/tuning.html`** — 新增 12 个隐藏 input + JS prefill
- **`app/routes/training.py:595-637`** — POST handler 新增 `action=create_job` 分支
- **`app/templates/training/tuning.html:407-423`** — `onTuningCompleteForTuningPage` 调优完成自动创建训练任务

## 当前状态
- 146 个模型，全部 trained，0 draft，0 无描述
- 556 个训练任务（部分无关联 model，因清理时删了）
- 375 测试全部通过

## 待办/未完成
- 还有一些 90%+ 准确率模型未深入审查（钢板缺陷检测-svm 99.5% 等），诊断脚本已通过健康度评分监控
- 调优页面的 `start_training` 功能是新加的，需要实际运行验证
